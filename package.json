{
  "name": "sistematriunfo01",
  "version": "1.0.0",
  "description": "Triunfo System - Gestao de Frota",
  "main": "server/server.js",
  "scripts": {
    "dev": "node server/server.js",
    "start": "node server/server.js",
    "prestart": "prisma migrate deploy --schema=server/prisma/schema.prisma || prisma db push --schema=server/prisma/schema.prisma",
    "postinstall": "prisma generate --schema=server/prisma/schema.prisma"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "prisma": "^5.14.0"
  }
}