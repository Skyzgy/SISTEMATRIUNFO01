// Other code before line 306

// Function definitions and other contents remain unchanged

function loadAdminReq() {
    buildTable(box);
    buildFooter(box);
    // buildToolbar(box); // This line has been removed
}

// buildToolbar function definition remains intact
function buildToolbar(box) {
    // implementation of buildToolbar
}
